const Preferences = () => {
  return (
    <div
      className="preferences-tab"
      aria-live="polite"
      aria-label="Preferences Settings"
    >
      Preferences Settings will be here soon.
    </div>
  );
};

export default Preferences;
